#pragma once

/**
 * Single source of truth for the application version shown in Help > About and
 * in the changelog header. Keep it in sync with the top released entry of
 * CHANGELOG.md.
 */
namespace AppVersion {
constexpr const char* kNumber = "1.1.0";
}
